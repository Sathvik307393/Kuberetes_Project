apiVersion: v1
kind: Service
metadata:
  name: {{ include "nutrition-service.fullname" . }}
  labels:
    {{- include "nutrition-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nutrition-service.selectorLabels" . | nindent 4 }}
