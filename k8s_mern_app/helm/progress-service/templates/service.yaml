apiVersion: v1
kind: Service
metadata:
  name: {{ include "progress-service.fullname" . }}
  labels:
    {{- include "progress-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "progress-service.selectorLabels" . | nindent 4 }}
