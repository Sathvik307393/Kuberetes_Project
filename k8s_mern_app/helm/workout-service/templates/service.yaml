apiVersion: v1
kind: Service
metadata:
  name: {{ include "workout-service.fullname" . }}
  labels:
    {{- include "workout-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workout-service.selectorLabels" . | nindent 4 }}
